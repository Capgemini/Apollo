# The following whitelist enables routes to Endor
# NOTE: We use `path_beg` to match, so anything under the path will route.

# app-level route prefixes
/locomotive
/assets/font-awesome
/assets/locomotive
/assets/select
/images/dynamic
/steam/dynamic

# special CMS route prefixes, e.g. for New Relic ping endpoint
/endor

# safe CMS route prefixes
/nanodegrees
/catalog/
