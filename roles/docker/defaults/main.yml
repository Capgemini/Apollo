---
# defaults file for docker
docker_tmp_dir: /var/lib/docker/tmp
docker_dns_config: "--dns=172.17.42.1 --dns=8.8.8.8 --dns-search='service.{{ consul_domain }}'"
docker_storage_config: --storage-driver=overlay
