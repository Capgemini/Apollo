---
# handlers file for docker
- name: reload systemd
  sudo: yes
  command: systemctl daemon-reload
  notify:
    - restart docker

- name: restart docker
  sudo: yes
  service:
   name: docker
   state: restarted
