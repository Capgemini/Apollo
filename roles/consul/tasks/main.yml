---
# Consul tasks

- include: config.yml
- include: server.yml
- include: client.yml
- include: service.yml

# Give some time for leader election to occur
- name: wait for leader
  wait_for:
    host: "{{ consul_bind_addr }}"
    port: 8301
    delay: 10
  tags:
    - consul
